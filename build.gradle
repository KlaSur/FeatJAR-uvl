plugins {
    id 'de.featjar.java-library'
}

dependencies {
    api 'de.featjar:formula'
    api 'de.featjar:feature-model'
	api group: 'io.github.universal-variability-language', name: 'fm-metamodel', version: '1.1'
    api testFixtures('de.featjar:formula')
    testImplementation 'de.featjar:formula-analysis-sat4j', 'de.featjar:formula-analysis-javasmt'
}

license {
    ext {
        licence_url = 'https://github.com/FeatureIDE/FeatJAR-uvl'
    }
}
